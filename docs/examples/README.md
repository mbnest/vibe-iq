# Worked Examples

A real (if illustrative) ask run through the front-of-flow artifact chain, one level at a time — a concrete reference the abstract docs can point to, and a stable fixture for later A/B comparison (alternate `spec.md` schemas, agent prompts) against a known-good chain.

| Example | What it is | Status |
|---|---|---|
| [`govern-iq/`](govern-iq/) | An enterprise data-quality & governance platform. Reverse-translated from existing L3 technical notes up to L1, then worked back down. Exercises *upward* translation — the direction the pipeline barely does. | Brief (L1) and product definition (L2) drafted; solution spec (L3) not started |

These are test fixtures for the pipeline design, not commitments to build the example systems.
