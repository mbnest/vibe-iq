source "https://rubygems.org"

# Matches the GitHub Pages build environment. For local preview:
#   cd docs && bundle install && bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"
